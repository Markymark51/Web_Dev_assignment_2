<%- include('partials/header.ejs') %>
<div class="container mt-4">
    <h1>Admin Panel</h1>
    <p>Welcome, Admin!</p>
    <h2>Registered Users</h2>
    <div class="list-group">
        <% users.forEach(user => { %>
            <%- include('partials/users.ejs', { user: user }) %>
        <% }); %>
    </div>
</div>
<%- include('partials/footer.ejs') %>